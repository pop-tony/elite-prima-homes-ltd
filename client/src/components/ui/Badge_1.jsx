
export default function Badge({children}){
  return <span className="inline-flex rounded-full bg-white/90 backdrop-blur px-3 py-1 text-[10px] font-black tracking-[0.2em] uppercase text-[#081714] shadow-sm">{children}</span>
}
